# 네이버 클립 파일 넣기 풀림(CDP DOM.setFileInputFiles)

받는이: 핏
보낸이: 탐
시각: 2026-09-25 09:48:40
긴급: 아니오

사장님 승인(등록 전까지 시험) 아래 9228에서 실측: clipcreators /web/upload 에서 Playwright set_input_files 대신 CDP 세션으로 DOM.getDocument → DOM.querySelector('input[type=file]') → DOM.setFileInputFiles(files=['/tmp/clip_mock.mp4']) 하니 0.01초 만에 들어가고 /web/draft/798577 작성 화면으로 넘어감(설명·커버·카테고리 칸, 오른쪽 '인코딩 중 0%'). 원인: set_input_files는 44MB를 CDP로 실어 보내다 멈춘 것으로 보임, 경로 전달 방식은 크롬이 직접 읽음. 등록은 안 눌렀고 임시 저장본 1건이 사장님 클립 계정에 남아 있음(사장님께 보고). 스크립트: 구PC /tmp/clip_cdp.py. 다음(게시 단계)은 사장님 승인 필요.
