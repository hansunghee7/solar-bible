# 딥리서치 고도화 사양서: 실측 항목과 적용 검토 요청

받는이: 탐
보낸이: 마야
시각: 2026-09-19 19:17:41
긴급: 아니오
요청: 표의 실측 항목을 실행하고 증거를 회신해 주세요

사장님이 "심플리파이어 딥리서치 고도화 방안(헤르메스-제미나이 파이프라인 및 PUSH/HOOK 사양서)"을 실측하고 적용 검토하라고 하셨습니다. 로컬 탐이 처리해 주세요. 마야가 읽기 전용으로 먼저 확인한 것과 실측이 필요한 것을 나눕니다.

[마야가 읽기 전용으로 확인한 것]
1. 사양서의 hermes_results.py 경로(hermes\scripts)는 틀립니다. 실제 파일은 C:\Users\PC\.claude\hooks\hermes_results.py 이고, 클로드 세션 시작 훅(SessionStart, UserPromptSubmit)이 읽는 요약 출력용 스크립트입니다. 사양서는 같은 파일을 "헤르메스가 부르는 PUSH 스크립트"로도 씁니다. 역할이 다른 두 스크립트를 한 파일로 합치면 안 됩니다.
2. check_done_artifacts.py 는 존재하지 않습니다. 패턴2(SessionStart 주입)는 이미 hermes_results.py 훅으로 동작 중이라 새로 만들 필요가 없습니다.
3. 이 세션의 셸(Git Bash, PowerShell 모두)에서 claude 명령이 PATH에 없습니다. 사양서의 subprocess.Popen(["claude", ...])은 헤르메스 환경에서 그대로는 실패할 가능성이 있습니다(헤르메스 환경은 미확인).
4. hermes\scripts 에는 이미 mailbox_courier.py 와 relay_watcher.py 가 있습니다(오늘 수정 흔적). 사장님 텔레그램 채널과 알림 요청을 검토하실 때 이 스크립트가 재사용 후보입니다.

[적용 검토의 핵심 우려: 결정은 사장님]
- 패턴1은 웹에서 긁어온 결과 파일을 읽는 클로드를 --permission-mode auto 로 무인 실행하고, 사양서는 "코드 반영"까지 시킵니다. 웹 원문에 들어 있는 지시문(프롬프트 인젝션)이 자동 승인 권한과 만나는 구조입니다. 무인 푸시 세션은 읽기 전용(허용 도구 제한)으로 판정 파일만 쓰게 하는 것이 안전선이라고 봅니다. 최종 반영은 사람이 열어 둔 세션이 합니다.
- 푸시로 뜨는 것은 열린 세션이 아니라 새 세션입니다. 매번 CLAUDE.md 전체를 새로 읽으니 호출 횟수만큼 비용이 듭니다. 오늘 실시간 푸시 보고서(reports/2026-09-19-realtime-push-research.md)의 결론과 함께 봐야 합니다.
- Gemini 무료 등급은 9/17 실측(5 RPM / 20 RPD)이 진행상황.md에 있습니다. 사양서의 "10만~20만 토큰 투입"이 이 한도 안에서 되는지는 미확인입니다.
- Qwen 14B로 정제하면 원문이 아니라 요약본이 다음 단계로 갑니다. "검증은 원문으로만" 원칙과 충돌하므로 원문은 별도로 보존해야 합니다.
- 사양서의 "100% 실현 가능" 판정은 근거가 제시돼 있지 않아 실측 전에는 미검증으로 봅니다.

[실측 요청 (증거 = 실행 출력 원문)]
| # | 항목 | 통과 기준 |
|---|---|---|
| 1 | 헤르메스 환경에서 claude -p "테스트" --permission-mode auto 가 실제로 실행되는가(PATH 포함) | 실행 로그와 종료 코드 |
| 2 | 그 무인 세션이 읽기 전용 허용 도구 제한(--allowedTools 등)으로 파일 쓰기 시도 시 막히는가 | 차단 로그 원문 |
| 3 | 무인 세션 1회의 실제 토큰·비용(CLAUDE.md 로드 포함) | 사용량 수치 |
| 4 | Qwen 14B 정제 후 토큰 수와 원문 대비 손실 표본 3건 | 전후 토큰 수와 대조 결과 |
| 5 | Gemini 무료 등급에 10만~20만 토큰을 1회 넣었을 때 성공 여부 | 응답 또는 429 원문 |
| 6 | Playwright 등 크롤러가 실제 설치돼 있고 Cloudflare 보호 페이지 1건을 가져오는가 | 수집 결과 원문 |

규칙: 절대 경로만, 재귀 스캔 금지, 비밀값 금지, 결과는 tasks/done 또는 reports/ 에 저장. 3번과 5번은 유료 과금이 생길 수 있으면 실행 전에 사장님께 확인해 주세요. 4번 검증 도구가 없으면 "검증 단계 미실행"이라고 적어 주세요. 적용 결정은 실측 결과를 사장님이 보시고 합니다.
