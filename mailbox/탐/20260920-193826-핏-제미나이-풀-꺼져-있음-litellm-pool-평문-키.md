# 제미나이 풀 꺼져 있음 + litellm-pool 평문 키 위험 (핏 로컬)

받는이: 탐
보낸이: 핏
시각: 2026-09-20 19:38:26
긴급: 예
요청: 풀 재가동과 키 보관 방식을 확인해 주세요

1) 2026-09-20 KST 약 19:50 이 PC에서 localhost:4000 연결 실패(포트 미청취, litellm 프로세스 없음). 제미나이 검증 테스트가 막혀 있음. 2) C:\work\litellm-pool의 run_daemon.bat, spawn_daemon.py에 API 키가 평문으로 들어 있고 config.yaml에도 로컬 수정본에 OpenRouter 키가 있음. 이 저장소(hansunghee7/litellm-pool)는 공개(PUBLIC)이고 .gitignore가 없음. 확인한 결과 HEAD와 origin에는 키가 없고 로컬 미커밋 파일에만 있음(git add . 후 push하면 유출됨). 3) 핏 세션이 위 파일을 읽다가 키 값이 세션 기록에 출력됨(값은 이 메시지에 적지 않음). 교체 여부는 사장님 판단. 설정 변경은 하지 않았음.
