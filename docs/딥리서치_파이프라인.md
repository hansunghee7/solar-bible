# 딥리서치 파이프라인 (실측 반영판, 2026-09-19)

Claude 토큰을 아끼기 위해 웹 수집과 대량 파일 처리는 헤르메스가 하고, 클로드 에이전트는 결과를 읽고 판정만 한다.
이 문서는 사장님이 준 기술 사양서를 **실제 구현 상태와 대조해서** 정리한 것이다. 없는 기능을 있다고 쓰지 않는다.

## 1. 구조 (5단계)
1. 클로드 에이전트가 발주한다(동기 또는 비동기).
2. 헤르메스가 웹 조사와 자료 수집을 한다. 주장마다 출처 URL과 원문 인용(15단어 이내)을 붙인다.
3. 제미나이가 검증한다. 자체 리서치는 시키지 않고, 헤르메스가 가져온 출처와 주장을 대조만 한다(`docs/AI_POOL_제미나이_검증_프로세스.md`).
4. 결과를 `tasks/done/` 또는 지정한 보고서 경로에 저장한다.
5. 의뢰한 클로드 에이전트가 결과를 읽고 증거를 확인한 뒤 최종 판정한다.

## 2. 구현 상태 (탐 실측)
| 항목 | 상태 | 근거 |
|---|---|---|
| 동기 발주(`hermes chat --oneshot --query "..."`) | **동작** | 왕복 약 12초 실측. 긴 조사는 백그라운드로 실행하고 결과를 파일로 받는다 |
| 비동기 발주(`tasks/pending/` 파일, 3분 폴러) | **동작** | 오늘 지시서 다수 처리 확인 |
| 결과 표준 서식(TASK/EXECUTION/OBSERVATION/VERIFICATION/STATUS) | **동작(권장)** | 지시서 공통 규칙에 포함, 완료 파일 대부분 준수 |
| 새 결과 알림: 클로드 세션 시작 훅 | **동작** | `hermes_results.py`가 새 `tasks/done` 결과를 요약 출력. 이미 열린 세션에는 즉시 반영되지 않음 |
| 긴급 알림: 우편함 `--urgent` → 텔레그램 | **동작** | 사장님 폰 도착 확인(2026-09-19) |
| `hx` 래퍼(명령 한 줄 동기 발주) | **미구현** | 실행 파일 없음. 백로그 |
| 업로드 이벤트 훅(`hermes_results.py --event ... --target ...`) | **미구현** | 스크립트가 인자를 받지 않음. 헤르메스가 업로드 시 호출하는 장치도 없음 |
| `tasks/done` 신규 파일 즉시 감지(파일 감시) | **미구현** | 감시 장치 없음. `FileChanged` 훅 시험이 백로그 |
| 제미나이 검증 자동 실행 도구 | **미확인** | 저장소·헤르메스 스크립트·스킬에서 검증 실행 도구를 찾지 못함. 도구가 없으면 보고서에 "검증 단계 미실행"이라고 적는다 |

## 3. 지금 쓰는 방법
- **짧은 조사(약 1분 이내)**: `hermes chat --oneshot --query "<지시문>"`. 지시문에 절대 경로와 산출물 경로를 적는다.
- **긴 조사(15턴 이상)**: 위 명령을 백그라운드로 돌려 출력을 파일에 저장하거나, `tasks/pending/`에 지시서를 넣는다.
- **결과 받기**: 지정한 경로의 파일을 직접 읽는다. 클로드 세션은 시작 때 훅이 새 결과를 알려 준다. 급하면 우편함에 `--urgent --ask "..."`로 사장님께 알린다.

## 4. 지켜야 할 규칙
1. 헤르메스에게는 **절대 경로만** 준다. 넓은 범위 재귀 스캔 금지.
2. 검증은 결과 파일의 **원문 출력**(해시, 로그, 표)으로만 한다. "완료했다"는 말은 증거가 아니다.
3. D 드라이브(HDD)의 자료는 삭제·이동하지 않는다. C 드라이브 여유 공간은 15% 이상 유지한다(§10).
4. 공개 저장소이므로 키, 토큰, 호스트명, IP, 개인정보를 결과에 쓰지 않는다.
5. 완료 보고에는 `python C:\Users\PC\AppData\Local\hermes\scripts\evidence_snapshot.py <저장소 경로>` 출력을 붙인다.
6. **열람하지 못한 URL은 "열람 실패"로 표기한다.** 검색 결과의 스니펫(미리보기 문구)만 보고 인용하지 않는다. 인용은 실제로 연 페이지의 원문에서만 가져온다. (근거: 마야의 628번 사실검증 첫 실전, 2026-09-19. FastCompany가 403이었는데 원작자 귀속 주장이 보고서에 들어가 재현되지 않았다.)
7. **직함, 날짜, 귀속(누가 한 말인가)처럼 세부 사실은 출처 둘이 일치할 때만 확정으로 쓴다.** 하나뿐이거나 서로 다르면 "미확정"으로 표기한다.
8. 사람이 표본으로 대조할 수 있도록, 각 주장에 `열람 성공/실패`, 접속 시각, 원문 인용을 같은 줄에 적는다(6-2절 Grounding 서식).

## 5. 미구현 항목의 우선순위 (백로그)
1. `hx` 래퍼: 동기 발주를 명령 한 줄로 하고 결과를 자동 저장한다.
2. 업로드 완료 알림: 헤르메스가 결과 저장 후 우편함으로 알리는 방식이 가장 단순하다(이미 있는 우편함 재사용).
3. 제미나이 검증 도구: 주장별 출처 대조를 실행하는 스크립트(verdict JSON)를 만들고, 없으면 "미실행"을 보고서에 기록한다.
4. 열린 세션에 즉시 알리는 방법: 딥리서치 보고서(`reports/2026-09-19-realtime-push-research.md`)의 결론에 따라 선택한다.

## 6. 고도화 검토 (2026-09-19 실측 반영)
사장님이 준 "고도화 사양서"의 전제를 이 PC에서 실측하고, 실현 가능한 것만 채택한다.

### 6-1. 실측 결과
| 사양서의 전제 | 실측 | 판정 |
|---|---|---|
| 로컬 Qwen 14B가 초당 100토큰 이상 | 84 tok/s (짧은 시험 1회, 모델이 이미 올라온 상태) | 거의 맞음. 정제 용도로 충분 |
| Playwright로 안티봇 우회 | 시스템 Python에 `playwright` 실행 파일 있음. 헤르메스 런타임에서 쓸 수 있는지는 확인 못 함 | 조건부 가능 |
| Exa, Firecrawl, Tavily 래퍼 | 헤르메스 설정에 키가 없음(주석 처리). 가입과 키 발급은 사장님 몫 | 지금은 보류 |
| 제미나이 무료 계정 풀로 검증 | 계정 풀 래퍼(`gemini-a1`~`a3`)는 있으나 **실행 오류**: 실제 패키지(`@google/gemini-cli`)가 없음. 전역 npm 폴더가 9/18 17:16에 비워짐(같은 날 헤르메스 정화 기록: `npm_global` 760MB 삭제)으로 보임 | **재설치(v0.60.0, 사장님 승인)는 성공했으나 무료 경로가 막힘.** 구글이 "이 클라이언트는 더 이상 지원되지 않는다(UNSUPPORTED_CLIENT), Antigravity로 이동하라"고 응답. 계정 a2, a3는 로그인 정보도 없음. 제미나이 CLI 계정 풀은 **사용하지 않는다** |
| `claude -p`로 헤르메스가 클로드를 깨움 | `claude` 명령이 이 PC에 없음(위 정화의 영향일 수 있음, 확인 못 함). 있어도 새 세션을 띄워 사용량을 쓰고 열린 세션에는 닿지 않음 | 채택하지 않음 |
| 100% 실현 가능 | 위 항목 때문에 성립하지 않음 | 과장 |

### 6-2. 채택하는 고도화
1. **HTML 정제는 라이브러리로 먼저**: 본문 추출 라이브러리(결정론적)로 HTML을 텍스트로 만들고, Qwen은 요약과 압축에만 쓴다. LLM으로 HTML을 벗기면 느리고 오류가 난다.
2. **Grounding 서식**: 결과 파일의 근거는 한 줄에 `주장 | URL | 접속 시각 | 원문 인용(15단어 이내)`로 적는다. 접속 시각이 없으면 재현할 수 없다.
3. **입력 크기 가드**: 제미나이에 넣기 전에 토큰 추정치가 15만을 넘으면 나눠서 넣는다(무료 한도 초과 방지).
4. **검증 스크립트 `verify_claims.py`**: 주장 목록(JSON)과 출처 본문을 입력으로 받아 `confirmed | unsupported | contradicted`를 낸다. 제미나이 호출은 헤르메스가 이미 쓰는 제미나이 API 경로(`--provider gemini`)를 우선하고, 보조로 안티그래비티 CLI(`agy`, 출력 모드와 JSON 스키마 지원)를 검토한다.
4. **판정 기준**: 검증 결과와 무관하게 최종 승인은 클로드가 원문 표본 3건을 열어 대조한 뒤에만 한다.

### 6-3. 선행 조건과 결정 대기
- (완료) 제미나이 CLI 재설치는 했지만 구글 정책으로 무료 경로가 막혔다. 대체 경로는 위 4번.
- Exa, Firecrawl, Tavily 무료 키는 사장님이 가입해야 한다. 지금은 Playwright를 먼저 쓴다.
- 이 정화 사고의 교훈: 재설치 가능한 폴더도 삭제 전에 "무엇이 이것에 의존하는가"를 확인한다(전역 npm 폴더에는 실행 도구가 들어 있다).
