# 지시서: 폴러 결정적 실행기 회귀 시험 (백로그 31 검증) (탐 → 헤르메스)

받는 이: 헤르메스 / 발주자: 탐(CTO) / 발주일: 2026-09-22 / 사장님 승인: 2026-09-21 저녁(착수 가능)

## 배경
백로그 31이 지적한 사고: 2026-09-20 14:19 폴러(Solar Pro 4 무료)가 지시서 처리 중 done 이동
단계를 빼먹고 27자짜리 짧은 답으로 정상 종료해, 같은 지시서를 재처리하느라 20분을 낭비하고
후속 지시서가 20분 대기했다. 원래 요구는 "폴러의 마무리(완료 표시, done 이동, 커밋, 알림)를 AI
대신 스크립트가 하고 AI는 실제 작업만 하기"였다.

이 요구는 **이번 세션 이전에 이미 구현됐다**: `pending_poller.py`(헤르메스 홈
`scripts/pending_poller.py`, 저장소 사본 `hansunghee7.github.io` 레포
`scripts/ops/hermes-poller/pending_poller.py`, PR #925·#927)가 done 이동·git add·commit·push·
우편 알림을 전부 스크립트가 결정적으로 처리하고, AI(`hermes chat`)는 지시서 본문 처리 결과
텍스트만 돌려준다. 즉 AI 응답이 얼마나 짧든 done 이동 여부는 AI가 아니라 스크립트의
`process_one()` 반환값(성공 exit code 여부)에만 달려 있어, 이 사고가 구조적으로 재발할 수
없어야 한다(탐 판단, 미검증).

이 지시서는 새로 만드는 것이 아니라 **그 판단이 맞는지 원래 사고 시나리오로 실제로 재현해
검증**하는 것이다.

**재시도(2026-09-22 12시대) — 지시 범위를 좁힘**: 1차 시도는 "크론을 트리거하고 기다려라"는
지시 때문에, **자기가 지금 처리되고 있는 바로 그 크론 실행 상태를 반복 확인하는 자기참조
루프**에 빠져 메시지 한도로 끊겼다(실제 시험은 못 함). 이번엔 **트리거와 대기를 시키지 않는다.**
너는 파일만 만들고 끝낸다. 확인은 탐(부모 세션)이 별도로 한다.

## TASK (아주 짧게, 그것만)
`pending_poller.py`(헤르메스 홈 실행본)는 건드리지 않는다. 아래 파일 하나만 만들고 커밋·push
한 뒤 끝낸다 — 트리거하지 말고, 기다리지 말고, 확인하지 마라.

1. `C:\work\solar-bible\tasks\pending\2026-09-22-regression-short-answer-test.md` 파일을
   아래 내용 그대로 만든다:

   ```
   # 회귀 시험용: 아주 짧은 답만 내는 지시서 (백로그 31 검증, 탐)

   숫자 1만 답하고 다른 말은 하지 마라.
   ```

2. `git add tasks/pending/2026-09-22-regression-short-answer-test.md && git commit -m "test: 백로그 31 회귀 시험용 지시서" && git push origin main`을 실행한다.
3. 결과 보고: 파일 생성 여부와 커밋 해시만 짧게 적는다(TASK/EVIDENCE/STATUS만, 나머지 절은
   생략해도 된다). 그 외 아무것도 하지 마라.

## 끝나면
`python C:\work\solar-bible\mailbox\mailbox.py send 탐 "백로그 31 시험 파일 준비 완료" --from 헤르메스 --body "커밋 해시: ..."`
