# 대기 작업: solar-bible 문서를 오늘 실제 상태로 일치시키기

받는 이: 신솔라
상태: QUEUED
우선순위: 6
발주: 탐(로컬), 사장님 승인 2026-09-19
읽는 방법: 이 파일 전체를 읽고 수행한다. 요약본만 보고 수행하지 않는다.

작업 위치: C:\work\solar-bible (다른 클론에서 하지 말 것)
목표: solar-bible 문서를 2026-09-19 실제 상태와 일치시킨다. 기존 내용은 지우지 않는다.

절차:
1. docs/진행상황.md 의 "D드라이브 2차 백업 위치" 표에서 D:\Downloads 줄과 ComfyUI 줄이
   맨 앞 "||" 또는 맨 끝 "||" 로 되어 있으면 각각 "|" 하나로 고쳐 표 형식을 바로잡는다.
   줄의 내용은 바꾸지 않는다.
2. 같은 파일 맨 아래에 새 절을 추가한다(기존 절은 손대지 않는다):

## 2026-09-19 시스템 사실 기록 (탐 실측, 사장님 실행)
- 방화벽 22번(SSH): 인바운드 허용 규칙 2개의 허용 출발지를 "Tailscale 대역(100.64.0.0/10)과
  로컬 서브넷"으로 제한. 인터넷 출발지 차단. 사장님이 관리자 창에서 적용, 탐이 값 확인.
- AnyDesk: 제거됨(서비스와 7070 포트 없음 확인). 필요할 때 다시 설치.
- OLLAMA_MODELS 환경변수: 사용자·시스템 변수 모두 삭제. Ollama는 기본 위치(C 드라이브 SSD의
  사용자 .ollama\models)를 쓴다. 모델 11개(약 71GB)가 C에 있고, D:\OllamaModels는 비어 있으며
  쓰지 않는다. D는 HDD(약 185MB/s)라 모델 저장 위치로 맞지 않다.
- 페이지파일: 실제 값 최소 32,768MB, 최대 65,536MB. 9/18 기록의 16,384/32,768은 실제와 달랐다.
- 헤르메스 config.yaml agent.max_turns: 15 에서 30 으로 변경(헤르메스 재시작 후 반영).
- 스크립트: health_check.py(첫 실행 전인 크론은 FAIL이 아니라 WAIT로 표시)와
  evidence_snapshot.py(git log와 크론 요약 오류)를 탐이 수정. 원본은 *.bak_0919 로 보관.
  크론 health-check 등록(30분 주기, 스크립트 방식, LLM 없음).
- 디스크 속도(탐 실측): D(HDD) 약 185MB/s(ComfyUI 모델 파일), C(SSD) 약 1.9GB/s(Ollama 모델 파일).
- 구PC: 사장님이 Linux를 설치했고 SSH로 접근 중. Tailscale에는 아직 다시 가입하지 않음.
- 아직 확인하지 못한 것: 공유기의 DMZ·UPnP 설정(사장님이 후순위로 확인 예정).

3. 커밋 대상은 docs/진행상황.md 하나뿐이다. 미추적 파일(_power_check.ps1, temp_pagefile.ps1,
   _poller_probe.py 등)은 커밋하지 않는다. 커밋 메시지는 "무엇을 했는가"만 쓴다.

완료 기준: 수정 전후 diff 원문, 커밋 해시, git ls-remote origin main 과 git log -1 해시 일치 원문.

완료 보고 마지막에 증거 스냅샷을 붙인다:
  python C:\Users\PC\AppData\Local\hermes\scripts\evidence_snapshot.py C:\work\solar-bible


---
## 공통 규칙 (이 지시서 전체에 적용)

보고 서식 (이 순서를 지킬 것, 칸을 생략하지 말 것)
TASK: 무엇을 하라고 했는가 (한 줄)
EXECUTION: 실제로 실행한 명령
OBSERVATION: 명령이 실제로 출력한 결과 (요약하지 말고 핵심 줄 그대로)
VERIFICATION: 완료 기준 각각에 대해 통과/실패
STATUS: DONE / 실패 / 일부만 완료 (셋 중 하나만)

- 작업 위치: 지시서에 적힌 절대 경로에서만 작업한다. 작업 전에 그 경로에서
  git status -sb 원문을 붙인다. 새 클론을 만들지 않는다.
- "running", "실행 중", "예약됨"은 완료가 아니다. 결과 로그나 출력 원문이 있어야 DONE이다.
- 금지 표현: "적용 완료(변경 안 함)"처럼 뜻이 부딪히는 문장. 바꿨으면 "바꿈", 안 바꿨으면 "안 바꿈".
- 확인하지 못한 것은 "확인 못 함"으로 쓴다. 추측을 사실처럼 쓰지 않는다.
- 비밀값(API 키, 토큰, 비밀번호)은 출력·기록하지 않는다. 보고서에도 쓰지 않는다.
- 이 저장소(solar-bible)는 공개다. 보고서와 커밋에 호스트명, IP 주소, 사용자 계정명,
  개인 파일명을 쓰지 않는다. 필요하면 <호스트>, <사용자>, <IP>로 표기한다.
- 파일·폴더 삭제, 이동, 덮어쓰기 금지 (이 지시서가 명시한 것 제외).
  D 드라이브 안의 어떤 것도 지우거나 옮기지 않는다.
- 파일·폴더 경로는 지시서에 적힌 것만 쓴다. 큰 파일을 찾으려고 C:\ 같은 넓은 범위를
  재귀 스캔하지 않는다. 경로가 없으면 스캔하지 말고 "경로 없음"으로 보고하고 멈춘다.
- 출처를 적을 때는 실제로 읽은 파일 종류를 그대로 쓴다(추측으로 채우지 않는다).
- 최종 보고 원문은 화면에만 두지 말고 C:\work\solar-bible\tasks\done\ 의 이 지시서
  파일 아래에도 남긴다.
- 막히면 멈추고 막힌 이유를 보고한다. 우회하지 않는다.
- 15턴 안에 못 끝나면 "여기까지 했고 다음은 이것"을 보고하고 멈춘다.
