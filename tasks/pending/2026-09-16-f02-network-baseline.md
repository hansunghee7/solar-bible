## 발주: 탐(로컬) → 신솔라

status: QUEUED

받는 이: 신솔라

## 배경

헤르메스 과제 백로그 우선순위(hansunghee7.github.io `docs/진행상황.md`
2026-09-14 8차 인수인계 "완료/우선순위 정리판" 순서 9번, 원래 ID F0-2)
채널 비교 실측(직접 호출 vs GitOps)을 위해 발주한다. **이 작업은 조사만
한다. 설정을 바꾸지 않는다.** "기본 통신 경로 결정"은 이번엔 하지 않고,
현황 조사까지만 한다.

## 작업 (전부 읽기 전용)

1. 이 PC(신솔라)가 다른 기기(다른PC, 노트북 등)와 통신할 때 실제로
   쓰는 경로가 Tailscale인지 LAN인지, 현재 설정과 실사용 흔적으로
   판단한다(추측이면 추측이라고 명시).
2. 내부 서비스(예: Ollama endpoint, Hermes Gateway)가 바라보는 주소가
   localhost/127.0.0.1인지, LAN IP인지, Tailscale IP인지 확인한다.
3. 공개 저장소이므로 실제 IP·MAC 주소는 적지 않는다. "Tailscale
   사용 중"/"LAN 사용 중"/"localhost 사용 중" 같은 판정만 적는다.

## 완료 기준

위 3개 항목을 이 파일 안에 "조사 결과"로 실제로 적은 뒤에만
`tasks/done/2026-09-16-f02-network-baseline.md`로 옮기고 git add·commit·
push한다. 빈 채로 옮기지 마라. 결정(경로 확정)은 이번 조사 결과를 보고
다음 발주로 별도로 낸다.
