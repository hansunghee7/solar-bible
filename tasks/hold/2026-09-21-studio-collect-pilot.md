# 지시서: 어사이드 브라우저의 유튜브 Studio 채널 분석 화면 읽기 파일럿 (탐 → 헤르메스)

받는 이: 헤르메스 / 발주자: 탐(CTO) / 발주일: 2026-09-21 / 실행 시각: 23:30(예약) / **1회만**
사장님 결정(2026-09-21): 어사이드(Aside) 브라우저 즐겨찾기에 로그인된 채로 열려 있는 쇼츠 채널 페이지를 새로고침해 최신 수치를 수집할 수 있는지 시험한다. 결과는 사람이 다시 확인한다.

## 목적
"화면 읽기로 Studio 수치를 안정적으로 모을 수 있는가"를 실측한다. 되면 정기 수집으로, 안 되면 공식 분석 API로 간다(탐이 판정). 이 시험은 **읽기 전용**이다.

## 허용되는 조작 (이것뿐)
1. 어사이드 창을 앞으로 가져오기(이미 열려 있으면 그대로). 왼쪽 즐겨찾기 목록에서 "Studio"가 들어간 항목이 있으면 그것을, 없으면 "신기한 아파트사전 - YouTube" 항목을 열고, 채널 화면 안의 "YouTube Studio" 또는 "채널 분석" 링크로 들어간다. 그래도 Studio 화면에 못 가면 주소창에 https://studio.youtube.com 만 입력해 이동한다(이 주소 하나만 입력 허용, 다른 글자 입력 금지).
2. 새로고침(F5 키 하나) 후 로딩 대기(최대 60초).
3. Studio 왼쪽 메뉴에서 "대시보드", "분석", "콘텐츠" 화면으로 이동하는 클릭, 화면 스크롤.
이 밖의 입력(글자 타이핑, 버튼 클릭, 설정·수익 창 열기)은 하지 않는다. **수정, 삭제, 업로드, 댓글, 구독, 설정 변경, 로그아웃, 창 닫기 금지.**

⚠️ **이 어사이드에는 사장님의 개인 SNS와 업무 계정이 전부 로그인된 채 즐겨찾기로 걸려 있다**(2026-09-21 사장님 확인: 인스타그램, 페이스북, 링크드인, 스레드, 틱톡 스튜디오, 리멤버, 지메일, 캘린더, 트렐로 등). 이 시험은 YouTube Studio 한 곳만 본다. 다음은 **어떤 경우에도 금지**: 그 밖의 즐겨찾기·탭을 클릭하거나 여는 것, 게시물 작성·좋아요·댓글·팔로우·메시지·메일 열람과 발송, "게시", "보내기", "공유" 같은 버튼 클릭. 클릭하기 전에 대상이 Studio 화면 안의 왼쪽 메뉴·새로고침·스크롤인지 확인하고, 조금이라도 의심스러우면 클릭하지 않고 멈춰 "막힘"으로 보고한다. 다른 사이트 화면이 앞에 나타나면 아무것도 누르지 말고 멈춘다.

## 즉시 멈추는 조건 (STATUS=막힘으로 보고하고 더 시도하지 않는다)
- 로그인 화면, 비밀번호, 2단계 인증, CAPTCHA, "본인 확인", 동의·약관 창이 뜬다. 어떤 것도 자동으로 통과하지 않는다.
- 계정 이메일이 화면에 보이는데 사장님 본인 계정(simon@simplifier.co.kr 또는 그 채널의 소유 계정)으로 확인되지 않는다. 계정 번호(/u/N/)가 아니라 화면에 표시된 이메일로 판단한다. 이메일 주소 자체는 보고서에 쓰지 않고 "일치/불일치"만 쓴다.
- 화면 조작 도구(computer_use 또는 browser)가 이 실행에 없다. 추측하지 말고 "도구 없음"으로 보고한다.

## 수집할 것 (화면에 보이는 것만, 안 보이면 "안 보임")
- 채널 요약: 구독자 수, 최근 28일 조회수, 최근 28일 시청 시간, 최근 28일 노출수와 노출 클릭률(있으면).
- 상위 콘텐츠 최대 5개: 영상 제목(앞 20자만), 조회수, 평균 시청 지속률(있으면).
- 각 수치 옆에 "화면에 표시된 기준 기간"(예: 최근 28일)을 같이 적는다. 기간 표기가 없으면 "기간 불명".

## 검증 (숫자를 믿기 전에)
`C:\work\hansunghee7.github.io\assets\data\youtube-insight.json`의 가장 최근 날짜 스냅샷과 대조한다: 구독자 수와 총 조회수, 상위 콘텐츠의 조회수(같은 영상 제목으로 찾기). 표로 「항목 | 화면 값 | API 값 | 일치 여부(조회수는 ±5%까지 일치로 봄, 구독자는 ±1%)」를 만든다. **대조 가능한 항목이 절반 미만이면 STATUS는 검증 불충분.**

## 보관 규칙
- 스크린샷이 필요하면 `C:\work\_ops\pilot\` 아래에만 저장한다(저장소 밖). 이 화면은 비공개 분석 수치라 공개 저장소에 스크린샷과 원본 수치 표를 올리지 않는다. solar-bible이 공개인지 모르므로 보고서에도 위 표의 수치는 「비율/일치 여부」 위주로 쓰고, 절대 수치는 `C:\work\_ops\pilot\studio-values-2026-09-21.txt`에 따로 저장한다.
- 키, 토큰, 이메일, 계정 표시는 어디에도 쓰지 않는다.

## 측정해서 보고서에 적을 것
소요 시간(시작~끝), 사람이 개입해야 했던 지점, 오류·막힘 내용, 읽은 수치 개수, 대조 가능 항목 수와 일치 수, 새로고침 후 값이 바뀌었는지(바뀌었다면 무엇이).

## 산출물과 보고
1. 보고서: `C:\work\solar-bible\reports\2026-09-21-studio-collect-pilot.md` (TASK/EXECUTION/OBSERVATION/VERIFICATION/EVIDENCE/FAILURE-RECOVERY/STATUS 형식).
2. (터미널 도구가 없는 유인 시험이면 이 우편 단계는 건너뛴다. 보고서만 쓰면 된다.) 끝나면 우편: `python C:\work\solar-bible\mailbox\mailbox.py send 탐 "Studio 수집 파일럿 결과" --from 헤르메스 --body "reports/2026-09-21-studio-collect-pilot.md 확인. STATUS=..."` (긴급 아님, 사장님 앞 알림은 만들지 않는다).
3. 실패해도 1회 재시도까지만. 그 이상은 하지 않고 원인만 적는다. 판정은 하지 않는다(판정은 탐).


## 3차 시행 방식 (유인, 사장님이 신PC 앞에서 각 승인 요청을 직접 허용·거부)
사장님이 클릭, 새로고침, 스크롤은 수정·삭제가 아닌 위험 없는 조작이라 승인하기로 했다(2026-09-21 채팅). 다만 승인은 사장님이 화면 앞에서 요청마다 직접 누른다(자동 승인 플래그와 설정 변경은 쓰지 않는다). 사장님이 허용하는 것은 아래 「허용되는 조작」에 있는 것뿐이고, 그 밖의 요청은 사장님이 거부한다. 거부되면 그 시도를 접고 우회하지 말고 멈춘다. 현재 어사이드 화면이 Studio가 아니면(예: 리멤버 등 개인 계정 화면) 그 화면은 건드리지 않고, 왼쪽 즐겨찾기의 「(2) 신기한 아파트사전 - YouTube」 하나만 클릭해 채널 화면의 「YouTube Studio」 링크로 들어간다. 클릭이 20번을 넘으면 길을 잃은 것이니 멈춘다. 실행 시각은 지시서의 예약 시각이 아니라 시스템 시계의 실제 시각을 적고, 상위 콘텐츠는 화면에 보이는 순서와 값 그대로 적는다. 새 보고서는 `reports/2026-09-21-studio-collect-pilot-attempt3-supervised.md`, 수치는 `C:\work\_ops\pilot\studio-values-attempt3.txt`. 터미널·우편은 쓰지 않는다.

## (참고) 2차 시행 방식 (2026-09-21, 사장님이 어사이드 창을 띄워 두고 자리를 비움, 완료)
1차(무인 크론)는 창이 화면에 없고 승인자도 없어 막혔다(reports/2026-09-21-studio-collect-pilot-attempt1-cron-blocked.md). 2차는 창이 이미 화면에 떠 있다. **승인이 필요한 조작(창 앞으로 가져오기, 클릭, 키 입력, 스크롤, 새로고침)은 승인자가 없으므로 시도하지 않는다.** 승인 없이 되는 화면 읽기(capture)만 한다. 지금 화면에 보이는 것이 Studio가 아니면 아무것도 하지 않고 '화면이 Studio가 아님'으로 보고한다. 읽은 수치는 '새로고침을 못 한 지금 화면 상태'라는 것을 표시한다(최신 아님 가능). 승인 도구가 막히면 막힌 사실만 기록하고 우회하지 않는다. 새 보고서는 같은 경로에 덮어쓰지 말고 `reports/2026-09-21-studio-collect-pilot-attempt2.md`로 저장한다. 터미널·우편은 쓰지 않는다.
