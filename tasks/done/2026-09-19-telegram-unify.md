# 지시서: 사장님 알림 채널을 텔레그램으로 통일 (탐 → 헤르메스)

## TASK
사장님이 2026-09-19 직접 확인했다: **사장님에게 가는 알림은 텔레그램 한 곳으로 통일한다. 카카오톡(카카오톡 MCP 포함)은 쓰지 않는다. 에이전트끼리는 계속 우편함을 쓴다.** 이 지시서는 그 방침을 도구에 반영한다. 지침 출처: 마야 우편 `mailbox/탐/20260919-191339-마야-정정-...md`.

## 해야 할 일
1. **`mailbox_courier.py` 확장** (`C:\Users\PC\AppData\Local\hermes\scripts\mailbox_courier.py`, 수정 전 백업 `.bak_0919_unify`):
   - 받는이가 `사장님`인 메시지는 긴급 여부와 관계없이 텔레그램 한 줄로 보낸다. 형식: `📬 탐→사장님: <제목>` 한 줄(본문은 넣지 않는다). 요청(`요청:` 헤더)이 있으면 둘째 줄 `👉 ...` 하나만 덧붙인다.
   - 받는이가 `전체` 또는 다른 에이전트인 메시지는 지금처럼 `긴급: 예`만 텔레그램으로 보낸다.
   - 폭주 방지: 텔레그램 발송은 하루(한국 시간 기준) 20건 상한. 넘으면 그날 나머지는 저녁 21:00에 `📬 오늘 쌓인 우편 N건` 요약 1건으로 보낸다.
   - 지금 쌓여 있는 사장님 앞 안 읽은 우편은 개별로 보내지 말고 `📬 사장님 앞 우편 N건 쌓임` 요약 1건으로 처리하고 상태 파일에 기록해 중복 발송을 막는다.
   - 글 톤: `docs/퍼스널_브랜딩_가이드.md` 화법을 참고하되 발신자 표시(`탐→사장님`)와 숫자·경로는 그대로 둔다.
2. **카카오톡 사용처 조사(읽기 전용, 삭제·수정 금지)**: `C:\work\solar-bible`, `C:\work\hansunghee7.github.io`(읽기만), 헤르메스 `scripts`와 `cron/jobs.json`, `C:\Users\PC\.claude\settings.json`의 MCP 항목에서 카카오톡을 쓰는 곳을 표로 만든다(`파일:줄 | 용도 | 텔레그램 대체 방법`). 결과는 `C:\work\solar-bible\reports\2026-09-19-kakao-inventory.md`.
3. **수신 방향 조사(만들지 말고 조사만)**: 사장님이 텔레그램으로 보낸 글을 헤르메스가 받았을 때 그 내용을 우편함 파일(`mailbox/<받는이>/`)로 넘기는 방법이 있는지 조사한다(헤르메스 gateway 설정, 스킬, 훅 문서 근거). 있으면 방법과 위험, 없으면 "없음"이라고 쓴다. 결과는 `C:\work\solar-bible\reports\2026-09-19-telegram-inbound.md`. 판단은 탐과 사장님이 한다.

## 규칙
- 토큰, 키, IP를 화면과 보고서에 쓰지 않는다(공개 저장소).
- 시험은 사장님 텔레그램에 `[시험]`으로 시작하는 메시지 1건만 실제로 보내서 발송 성공 로그(HTTP 응답 코드)를 증거로 붙인다. 사장님 폰에 도착했는지는 사장님이 확인한다.
- 결과 보고 형식: TASK/EXECUTION/OBSERVATION/VERIFICATION/EVIDENCE/FAILURE-RECOVERY/STATUS. 완료했다는 말만으로 DONE 처리하지 않는다.
