## 발주: 탐 → 신솔라

status: QUEUED

받는 이: 신솔라

## 배경

OneDrive 문제는 지난 조사(`tasks/done/2026-09-14-shinsolar-cleanup-survey.md`)로
끝났다(연결 해제 상태, 충돌 흔적 없음). 사장님 새 보고: 이번엔 **구글
드라이브와 Hermes 때문에** 신PC 폴더·파일이 뒤죽박죽이라고 한다. **이
작업은 조사만 한다. 파일을 옮기거나 지우지 않는다.**

## 조사 항목 (전부 읽기 전용)

1. **구글 드라이브 동기화 폴더 확인**: 구글 드라이브(Google Drive for
   desktop 등) 동기화 대상 폴더 경로를 찾는다. 여러 개면 전부 나열한다.
2. **Hermes가 건드리는 폴더와 겹치는지 확인**: Hermes 작업 폴더(`~/.hermes`,
   `%LOCALAPPDATA%\hermes`, 작업 저장소 클론 위치 등, 지난 조사에서 이미
   나온 경로 참고)와 구글 드라이브 동기화 폴더가 겹치는 곳이 있는지 확인한다
   (예: Hermes가 구글 드라이브 폴더 안에서 git clone·파일 생성을 하고
   있는 경우).
3. **구체적 증상 확인**: 아래 흔적이 있는지 가볍게 훑어본다(로그를 뒤집어
   엎지 말고 흔한 위치만).
   - 구글 드라이브 동기화 충돌 파일명 패턴(예: "(1)", "충돌 사본",
     "conflicting copy" 등)
   - 같은 저장소·같은 파일이 여러 위치(C 드라이브 작업 폴더 + 구글
     드라이브 폴더)에 중복돼 있는지
   - 구글 드라이브 동기화가 멈췄거나 에러 상태인지(트레이 아이콘·로그
     확인 가능하면)
4. **원인 추정**: 위에서 확인된 사실만으로, "왜 뒤죽박죽인지"에 대한
   가장 그럴듯한 설명을 적는다(추측이면 추측이라고 명시, 솔라바이블
   §10 — 사실과 추정을 섞지 않는다).

## 완료 기준

위 4개 항목을 이 파일 안에 "조사 결과"로 적고 `tasks/done/`으로 옮긴 뒤
git add·commit·push까지 한다. 정리(파일 이동·삭제) 방법은 이 조사 결과를
보고 다음 발주로 별도로 낸다 — 이번엔 안 한다.

---

## 정정(2026-09-15, 탐): 실제로는 처리 안 됐다 — 재발주함

이 파일도 "조사 결과" 절 없이 원본 그대로 done으로 옮겨졌다. 같은
시각 구솔라 터미널 혼선(여러 줄 붙여넣기 interrupt)과 겹쳐, 그 와중에
실제 조사 없이 파일만 옮겨진 것으로 보인다(추정). 재발주:
`tasks/pending/2026-09-15-gdrive-hermes-file-mess-survey-retry.md`.
