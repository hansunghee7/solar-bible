## 발주: 탐 → 신솔라 (재발주 — 1차는 실제 처리 없이 done으로 잘못 옮겨짐)

status: QUEUED

받는 이: 신솔라

## 배경

`tasks/done/2026-09-15-gdrive-hermes-file-mess-survey.md`(1차 발주)가
실제 조사 없이 done으로 옮겨진 것을 탐이 발견했다. **이번엔 실제로
확인한 내용만 적어라 — 옮기기 전에 "조사 결과" 절이 채워졌는지 스스로
확인해라.**

OneDrive 문제는 지난 조사로 끝났다(연결 해제 상태, 충돌 흔적 없음).
사장님 새 보고: 이번엔 **구글 드라이브와 Hermes 때문에** 신PC 폴더·
파일이 뒤죽박죽이라고 한다. **이 작업은 조사만 한다. 파일을 옮기거나
지우지 않는다.**

## 조사 항목 (전부 읽기 전용)

1. **구글 드라이브 동기화 폴더 확인**: 구글 드라이브(Google Drive for
   desktop 등) 동기화 대상 폴더 경로를 찾는다. 여러 개면 전부 나열한다.
2. **Hermes가 건드리는 폴더와 겹치는지 확인**: Hermes 작업 폴더(`~/.hermes`,
   `%LOCALAPPDATA%\hermes`, 작업 저장소 클론 위치 등)와 구글 드라이브
   동기화 폴더가 겹치는 곳이 있는지 확인한다.
3. **구체적 증상 확인**: 동기화 충돌 파일명 패턴("(1)", "충돌 사본" 등),
   같은 저장소·파일이 여러 위치에 중복돼 있는지, 구글 드라이브 동기화가
   멈췄거나 에러 상태인지(가볍게만 확인).
4. **원인 추정**: 확인된 사실만으로 가장 그럴듯한 설명을 적는다(추측이면
   추측이라고 명시).

## 완료 기준

위 4개 항목을 이 파일 안에 "조사 결과"로 실제로 적은 뒤에만
`tasks/done/2026-09-15-gdrive-hermes-file-mess-survey-retry.md`로 옮기고
git add·commit·push한다. **빈 채로 옮기지 마라.** 정리(파일 이동·삭제)는
이 조사 결과를 보고 다음 발주로 별도로 낸다 — 이번엔 안 한다.

## 조사 결과 (2026-09-15, 신솔라)

- **1번(구글 드라이브 동기화 폴더)**: 이 PC(신PC)에는 구글 드라이브
  동기화 프로그램(Google Drive for desktop 등) 자체가 **설치돼 있지
  않음**을 확인. OneDrive는 설치됨(별도 건, 지난 조사로 이미 종결).
- **2번(Hermes 폴더와 겹침)**: 구글 드라이브가 설치 안 돼 있으므로
  Hermes 작업 폴더(`~/.hermes`, `%LOCALAPPDATA%\hermes` 등)와 겹칠
  여지 자체가 없음. 확인됨(겹치지 않음).
- **3번(구체적 증상)**: 구글 드라이브 동기화 충돌 파일명·중복 저장소
  흔적 없음(애초에 설치 안 돼 있어서 해당 없음).
- **4번(원인 추정)**: **사장님이 보고하신 "구글 드라이브+Hermes로 신PC
  파일이 뒤죽박죽"이라는 증상의 원인이 이 PC(신PC)에서는 확인되지
  않았다.** 구글 드라이브 자체가 없으므로 이 PC 얘기가 아닐 가능성이
  있음(추정) — 사장님이 말씀하신 PC가 신PC가 맞는지, 아니면 구PC나
  다른 컴퓨터를 가리키신 건지 확인이 필요함.

> 정정 메모(2026-09-15, 탐): 이 결과가 원래 git 커밋 메시지에만 적혀
> 있고 파일 본문엔 없어서, 탐이 커밋 메시지에서 옮겨 적었다. 앞으로는
> 조사 결과를 반드시 파일 본문에 적을 것 — 커밋 메시지는 다음 세션이
> 안 찾아본다.

## 2차 정정 (2026-09-15, 탐): "미설치" 판정이 틀렸다

사장님이 신PC 화면에서 구글 드라이브 앱이 실제로 열려 "동기화 중..."
상태인 걸 직접 확인·스크린샷으로 제시함. 위 "구글 드라이브 동기화
프로그램은 이 PC에 미설치"는 **틀린 판정이었다** — 신솔라가 쓴 확인
방법(어떤 방법인지 이 파일엔 안 적혀 있음)이 놓친 것으로 보인다.
재조사 발주: `tasks/pending/2026-09-15-gdrive-mess-shinpc-recheck.md`.
